services:
  app:
    build: .
    container_name: stook-browser
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      AUTO_CREATE_BUCKET: "false"
    volumes:
      - ./public:/app/public:ro
    restart: unless-stopped
